{
  "name": "@atlaserp/sdk",
  "version": "0.0.0",
  "private": true,
  "description": "SDK interno para consumir la API desde apps cliente",
  "main": "./src/index.js",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}

