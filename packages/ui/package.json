{
  "name": "@atlasrep/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Componentes reutilizables de UI — React + TailwindCSS 4.1",
  "main": "./src/index.js",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
