# T-0313 — Configurar scripts de build por app

- **Estado**: CERRADA
- **Fecha**: 2025-07-11

## Entregables

| App      | Script build     |
| -------- | ---------------- |
| api      | `nest build`     |
| web      | `vite build`     |
| worker   | `nest build`     |
| desktop  | `tauri build`    |

Todos los `echo 'TODO: ...'` de build reemplazados por comandos reales.
